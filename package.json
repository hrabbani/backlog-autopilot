{
  "name": "backlog-autopilot",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:orchestrator": "bun --filter orchestrator dev",
    "dev:control-plane": "bun --filter control-plane dev",
    "build": "bun --filter '*' build",
    "setup": "bun --filter orchestrator setup",
    "sweep": "bun --filter orchestrator sweep"
  },
  "dependencies": {
    "@linear/sdk": "^80.1.0"
  }
}
